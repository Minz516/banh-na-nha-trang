{
  "name": "banh-trang-nha-na",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "seed:admin": "pnpm --filter api seed:admin",
    "seed:catalog": "pnpm --filter api seed:catalog",
    "seed:blog": "pnpm --filter api seed:blog",
    "seed:all": "pnpm --filter api seed:admin && pnpm --filter api seed:catalog && pnpm --filter api seed:blog"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.9"
}
